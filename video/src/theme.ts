/**
 * Visual language mirrors the live site (light, monochrome, black pill
 * buttons) so the video and the product read as one thing. Amber is the
 * only annotation color — reserved for the on-chain proof highlights.
 */
export const theme = {
  bg: '#F7F7F5',
  ink: '#0A0A0A',
  muted: '#6B7280',
  amber: '#F59E0B',
  green: '#16A34A',
  pillBg: 'rgba(10,10,10,0.88)',
  pillText: '#FFFFFF',
  font: "-apple-system, BlinkMacSystemFont, 'Segoe UI', 'Inter', sans-serif",
  mono: "'SF Mono', ui-monospace, Menlo, Consolas, monospace",
} as const;

export const FPS = 30;
